# ProofOps UI env template (copy to .env.local; never commit real keys).
# Empty VITE_PROOFOPS_API_KEY = unauthenticated demo mode (mutating calls
# fail closed server-side with 401; the Safety Gate shows a notice).
VITE_API_URL=http://localhost:8000
VITE_PROOFOPS_API_KEY=
